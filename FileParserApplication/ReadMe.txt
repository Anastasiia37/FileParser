The application has two modes:
1. Calculate the number of occurrences of a line in the specified text file.
2. To replace the string with another in the specified text file.
The application is started from the command line.

Input parameters:
1. <file path> <line to count>
2. <file path> <search string> <replacement string>